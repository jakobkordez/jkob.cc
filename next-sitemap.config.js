/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://jkob.cc',
  generateRobotsTxt: true,
  generateIndexSitemap: false,
  sitemapSize: 1000,
  sitemapBaseFileName: 'sitemap_index',
};

module.exports = config;